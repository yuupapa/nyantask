# 引き継ぎ書 — 2026-05-15 セッション

> 次のAIエージェントセッションへの申し送りです。

---

## このセッションでやったこと（不採用）

**VOICEVOXずんだもん歌唱 技術検証**

- `scripts/voicevox-singing-test.mjs` を作成
- SONG モード（v0.14+ の `sing_frame_audio_query` / `frame_synthesis`）とピッチ操作モードの2方式を実装
- **結論：にゃんタスクでは使わないことになった**

### 後処理が必要なもの

- **PR #33 はクローズ推奨**（`claude/voicevox-zundamon-singing-9yfS8` ブランチ、技術検証のみ）
- `scripts/voicevox-singing-test.mjs` は不要なら削除してよい

---

## プロジェクト現状（Phase 1.6 完了）

### 実装済み機能

| フェーズ | 内容 |
|---------|------|
| 1.1 | 管理画面（layout/dashboard/users/invitations）、ロール変更、UI日本語化 |
| 1.2 | タスクCRUD（ルーチン/デイリー）、コイン付与、RLS検証 |
| 1.3 | `cats` テーブル（960通り）、decayロジック、CatCard/各ボタン |
| 1.4 | コイン+にくきゅう二重通貨、なかよしLv、なでなで |
| 1.5 | `/cats` 図鑑、season.ts、季節背景・バッジ、複数猫管理 |
| 1.6 | PWA（manifest+sw.js）、Gemini API（猫トーク）、Web Push通知 |
| 追加 | pg_cron時間減衰、レア報酬枠、ストリークボーナス、猫名変更UI |
| 追加 | ショップ機能（餌3種・おもちゃ3種）、Gemini Vault移行 |
| 追加 | 管理画面ブロードキャスト通知UI |
| 追加 | 猫スプライトアニメーション（idle/walk/run 3種）、尻尾クリップ修正 |

### Supabase に手動適用が必要な可能性があるマイグレーション

- `20260503040000_gemini_apikey.sql`（profiles.gemini_api_key カラム）
- `20260503050000_push_subscriptions.sql`（push_subscriptions テーブル）
→ Supabaseダッシュボード > SQL Editor で確認・実行

---

## 次のアクション（優先度順）

1. **動作確認・テスト運用**（コンサル生10名への展開 = Phase 2 準備）
   - Vercelデプロイ確認
   - 実機（スマホ）でのPWAインストール確認
   - 各機能の結合テスト

2. **Supabase未適用マイグレーション確認**（上記参照）

3. **Phase 2 計画策定**（`docs/PLAN.md` の Phase 2 セクションを参照）

---

## 技術スタック

- Next.js 16.2.4 (App Router) + Tailwind CSS + Framer Motion
- Supabase（Auth/DB/Edge Functions/Vault）
- Gemini 2.5 Flash（ユーザー側APIキー、Vault管理）
- Vercel（ホスティング）
- PWA（next-pwa）

## ブランチ構成

| ブランチ | 状態 |
|---------|------|
| `main` | 本番相当（最新安定版） |
| `claude/voicevox-zundamon-singing-9yfS8` | PR #33（不採用、クローズ推奨） |

## 重要ファイル

| ファイル | 役割 |
|---------|------|
| `AGENTS.md` | 全エージェント共通ルール（憲法） |
| `CLAUDE.md` | Claude Code 用エントリポイント |
| `_PROJECT_STATE.md` | 現在の作業状態（詳細） |
| `docs/PLAN.md` | 企画書 v6 |

---

更新日：2026-05-15
